%h1 Users#index

%table
  %thead
    %tr   
      %th Username
      %th Email
      %th Type
  %tbody
    -@users.each do |user|
      %tr
        %td= user.username
        %td= user.email
        %td= user.user_type
